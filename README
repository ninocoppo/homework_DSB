Per fare funzionare correttamente l'applicazione eseguire i seguenti comandi
#Avviare minikube ed assegnare 4 GB di RAM
minikube start --memory 3000
#Eseguire questo comando per fare eventuali rebuild delle immagini 
eval $(minikube docker-env)
#Montare la cartella storage su minikube
minikube mount ./:/storage

