apiVersion: v1
kind: Service
metadata:
  name: prometheus-service
  labels:
    app: prometheus
spec:
  type: ClusterIP

  ports:
  - name: "innerport"
    port: 9090
    targetPort: 9090
  
  selector:
    app: prometheus
status:
  loadBalancer: {}
